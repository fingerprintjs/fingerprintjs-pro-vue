# Agent Notes

- For CI or workflow issues related to `fingerprintjs/dx-team-toolkit`, prefer working within the toolkit contract first.
  - Allowed options: use supported workflow inputs to work around the issue in this repo, or tell the user to fix the problem upstream in `dx-team-toolkit`.
  - Do not migrate this repo away from `dx-team-toolkit` as a workaround unless the user explicitly changes that constraint.
